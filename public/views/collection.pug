doctype html
html
  head
    title My Collection
    link(rel='stylesheet', href= "../css/collection.css")
  body
    // Top Bar
    .top-bar
      h1 My Collection
        a(href='/user', class='back-to-user') Back to User

    // Binders Row
    .binders-row
      each binder in binders
        .binder
          a(href=`/binder/${binder._id}`) #{binder.name}

    // Cards Grid
    .cards-grid
      each card in cards
        .card
          a(href=`/card/${card._id}`)
            img(src=`${card.image}` alt=card.name)
